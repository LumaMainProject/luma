import 'package:flutter/material.dart';

class PageBuyerPlayer extends StatelessWidget {
  const PageBuyerPlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}