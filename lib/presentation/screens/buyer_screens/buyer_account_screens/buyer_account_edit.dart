import 'package:flutter/material.dart';

class BuyerAccountEdit extends StatelessWidget {
  const BuyerAccountEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}