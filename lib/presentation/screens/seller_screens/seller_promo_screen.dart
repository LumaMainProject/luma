import 'package:flutter/material.dart';

class SellerPromoScreen extends StatelessWidget {
  const SellerPromoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Запуск проммо"),),
    );
  }
}